// Database connection will be configured
// when we reach the Azure Database step.

const connectDatabase = () => {
    console.log("Database connection will be configured later.");
};

module.exports = connectDatabase;